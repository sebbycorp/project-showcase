# Copy to terraform.tfvars (gitignored) or pass the same keys as TF_VAR_*.
# Do not put license keys or API keys in this file or in git.

project_id      = "maniak-showcase"
project_name    = "showcase"
org_id          = "629527910221"
billing_account = "011C38-867461-BE95B1"
create_project  = true

region       = "us-central1"
cluster_name = "showcase"

# Autopilot already installs Gateway API CRDs. Leave false unless the cluster
# does not already have Gateway API.
# manage_gateway_api_crds = false

# Never put real keys in this file or in git. Pass them at apply time:
#   export TF_VAR_agentgateway_license_key
#   export TF_VAR_openai_api_key
# Leave both empty on a first apply that only creates the cluster.
